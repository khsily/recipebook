import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
    },

    input: {
        marginHorizontal: 10,
        marginVertical: 10,
        padding: 10,
        borderWidth: 1,
        borderRadius: 3,
        borderColor: '#666666',
    },

    list: {
        
    },

    footer: {

    },

    submitButton: {
        width: '100%',
        height: 45,
        alignItems: 'center',
        justifyContent: 'center',
        padding: 0,
    },

    submitButtonText: {
        fontWeight: 'bold',
        fontSize: 18,
    },
});