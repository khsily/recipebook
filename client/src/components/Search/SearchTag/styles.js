import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        alignSelf: 'baseline',
        flexDirection: 'row',
        alignItems: 'center',
        borderWidth: 1,
        borderRadius: 5,
        padding: 8,
    },

    text: {
        marginRight: 12,
    },

    delete: {
        width: 12,
        height: 12,
    },
});